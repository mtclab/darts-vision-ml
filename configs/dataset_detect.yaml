# YOLO11-detect: dartboard + dart tip bounding boxes
# Run training from the repo root: python scripts/train_detect.py
path: /workspace/data/processed/yolo11_detect
train: images/train
val: images/val
test: images/test

names:
  0: dartboard
  1: dart_tip