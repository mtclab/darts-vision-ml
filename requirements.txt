# --- Already in ultralytics/ultralytics:latest base image (torch 2.11, CUDA 12.8) ---
# torch, torchvision, ultralytics, opencv-python-headless, numpy, Pillow, PyYAML,
# matplotlib, tqdm, pandas, scikit-learn, albumentations
# Do NOT reinstall — causes resolver to hang or break CUDA torch.

# --- VLM / NLP extras (not in base) ---
transformers==5.5.4
qwen-vl-utils==0.0.14
peft==0.19.1
multiprocess==0.70.19
datasets==4.8.4
moondream==0.2.1